# c1
```cpp
#define TEXT(quote) __TEXT(quote)   // r_winnt
#define __TEXT(quote) L##quote      // r_winnt

## 是预处理器运算符


例如

TEXT("hello")  预编译器编译成  L"hello"  

定义UNICODE 的时候 匹配 wchar_t 的API 


#include<windows.h>
windows.h 非常重要 包括了很多其他的windows头文件


#include <windef.h>  基本数据类型
#include <winnt.h> unicode 类型定义
#include <winbase.h> 内核函数
#include <winuser.h> 用户界面函数
#include <wingdi.h> 图形设备接口函数

```
### 程序入口
[程序入口](https://learn.microsoft.com/zh-cn/windows/win32/learnwin32/winmain--the-application-entry-point)
```cpp
int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow){

}

int WINAPI wWinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPWSTR LPWSTR, int nCmdShow){

}



```
